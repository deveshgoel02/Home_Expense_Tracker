# Base URL for the backend API. During local development, Vite proxies /api
# to the backend automatically (see vite.config.ts), so this is only needed
# for a production build served separately from the API.
VITE_API_URL=/api
