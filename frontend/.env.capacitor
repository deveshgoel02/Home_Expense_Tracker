# Used only for the Capacitor (packaged Android app) build — the app bundles
# static assets locally, so there's no dev-server proxy to fall back on the
# way there is for `npm run dev` or the Vercel deployment.
VITE_API_URL=https://family-expense-backend-mmrw.onrender.com/api
